import React from 'react';

export default function Form(props) {
    return(
        <div>
           <form>
               Form
           </form>
           {console.log(props)}
        </div>
    );
}